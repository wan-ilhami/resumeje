{
  "name": "resumeje",
  "version": "1.0.0",
  "private": true,
  "description": "Resume Builder - AI-powered resume enhancement",
  "author": "",
  "license": "ISC",
  "scripts": {
    "clean:ports": "bunx kill-port --port 3000,3001",
    "clean:cache": "bunx rimraf ../frontend/.next ../backend/dist",
    "clean": "bun run clean:cache && bun run clean:ports",
    "dev": "bun install && bun install --cwd ../backend && bun install --cwd ../frontend && bun run clean && concurrently -n BACKEND,FRONTEND -c green,cyan \"bun run --cwd ../backend dev\" \"bun run --cwd ../frontend dev\"",
    "lint": "bun run --cwd ../frontend lint && bun run --cwd ../backend lint",
    "lint:fix": "bun run --cwd ../frontend lint:fix && bun run --cwd ../backend lint:fix",
    "type-check": "bun run --cwd ../frontend type-check && bun run --cwd ../backend type-check",
    "verify": "bun run lint && bun run type-check",
    "verify:frontend": "bun run --cwd ../frontend lint && bun run --cwd ../frontend type-check",
    "verify:backend": "bun run --cwd ../backend lint && bun run --cwd ../backend type-check",
    "build": "bun run build:all",
    "build:all": "bun run verify && bun run --cwd ../backend build && bun run --cwd ../frontend build",
    "prod": "bun run --cwd ../backend start & bun run --cwd ../frontend start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "husky": "^9.1.7",
    "kill-port": "^2.0.1",
    "rimraf": "^6.1.3"
  },
  "overrides": {
    "brace-expansion": "^5.0.6",
    "lodash": "^4.17.24",
    "lodash-es": "^4.17.24",
    "undici": "^7.24.0",
    "handlebars": "^4.7.9",
    "fast-uri": "^3.1.2",
    "picomatch": "^4.0.4"
  }
}
