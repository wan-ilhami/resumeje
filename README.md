# 📘 RESUME BUILDER (v1.0.0)

## 🚀 Project Overview
A modern Resume Builder built with Next.js 16, React 19, Tailwind CSS v4, and Headless UI — designed to create professional, customizable, and ATS-friendly resumes with ease.
This project includes automatic versioning, Git tagging, and Vercel deployment integration for seamless CI/CD.

---

## 🚀 Features

✅ Next.js 16 (App Router)
✅ React 19 with Server Components
✅ Tailwind CSS v4 + Typography plugin
✅ Headless UI & Radix UI for accessibility
✅ Dark mode via next-themes
✅ PDF generation using html2pdf.js
✅ Auto version + Git tag injection on build
✅ Ready for Vercel production deployment

```


## 📂 Folder Structure

```
resume-builder/
├── app/
│   ├── layout.jsx               # Root layout
│   ├── page.jsx                 # Main page
│   ├── providers/page.jsx       # Global Providers (Theme, Redux, etc.)
│   └── globals.css              # Global styles
│
├── components/                  # Reusable React components
│   ├── Navbar.jsx
│   ├── Footer.jsx
│   ├── ResumeTemplate.jsx
│   └── FormSection.jsx
│
├── hooks/                       # Custom hooks
│   ├── useThemeSwitch.js
│   ├── useFormHandler.js
│   └── usePDFExport.js
│
├── scripts/
│   └── set-version.js           # Auto version + tag generator
│
├── public/                      # Static assets (logos, icons, images)
│   └── icon.svg
│
├── .env.dev                     # Development environment variables
├── .env.production              # Production environment variables (auto-generated)
├── package.json
├── tailwind.config.js
└── README.md

```

---

## ⚙️ Environment Configuration

You can define environment-specific variables for development and production.

**.env.dev**
```
NEXT_PUBLIC_ENV=development
NEXT_PUBLIC_APP_VERSION=1.0.0

```

**.env.production**
.env.production (auto-generated by scripts/set-version.js during build)
```
NEXT_PUBLIC_ENV=production
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_GIT_COMMIT=abc123
NEXT_PUBLIC_GIT_TAG=v1.0.0

```

---

## 🧠 Scripts Overview

```json
"scripts": {
  "dev": "next dev",
  "prebuild": "node scripts/set-version.js",
  "build": "next build",
  "start": "next start",
  "lint": "next lint",
  "release": "standard-version"
}

```

npm run dev → Start development mode

npm run build → Build for production (auto generates .env.production)

npm start → Run production build

npm run release → Bump version + tag + changelog

---

---

## 📦 Installation & Setup

1. **Clone the repo**
   ```bash
   git clone https://github.com/wan-ilhami/resume-builder.git
   cd resume-builder

   ```

2. **Install dependencies**
   ```bash
   npm install
   ```

3. **Run in development mode**
   ```bash
   npm run dev
   ```

4. **Build and start production**
   ```bash
   npm run build
   npm start

   ```

---

## 💡 Notes
---

## ⚙️ ⚙️ Auto Versioning Setup

Versioning is fully automated via scripts/set-version.js and standard-version.

### 1️⃣ Generate new version + changelog
```bash
npm run release
```

### 2️⃣ Push to GitHub with tags
```bash
git push --follow-tags origin main
```

### 3️⃣ Build process automatically:
Creates .env.production with Git commit & tag

Embeds version in your app

Triggers a Vercel redeploy

---

### 🧠 Example Workflow

#### 1️⃣ Work on your feature
```bash
git add .
git commit -m "feat: add new footer section"
```

#### 2️⃣ Bump version and changelog
```bash
npm run release
```

#### 3️⃣ Push everything to GitHub
```bash
git push --follow-tags origin main
```
